<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.psx.mapper.MovieMapper">
<!--    查询全部数据-->
    <select id="queryMovieList" resultType="Movie">
        select * from movie_list
    </select>

    <insert id="addMovie" parameterType="Movie">
        insert into movie_list (mviename,director,movietime,moviedes,moviecover,movie_stars)
        values (#{mviename},#{director},#{movietime},#{moviedes},#{moviecover},#{movie_stars})
    <selectKey resultType="Integer" order="AFTER" keyProperty="id">
        SELECT LAST_INSERT_ID() AS id
    </selectKey>
    </insert>
    <insert id="insertMovieAndType" parameterType="MovieAndType">
        insert into type_list (movieid,typeid) values (#{movieid},#{typeid})
    </insert>


    <update id="updateMovie" parameterType="Movie">
        update movie_list set id=#{id},mviename=#{mviename},director=#{director},movietime=#{movietime},moviedes=#{moviedes},moviecover=#{moviecover},movie_stars=#{movie_stars} where id= #{id}
    </update>

    <delete id="deleteMovie" parameterType="int">
        delete from movie_list where id=#{id}
    </delete>
</mapper>